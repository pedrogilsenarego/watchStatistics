import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
	palette: {
		primary: {
			main: "#48415a"
		},
		secondary: {
			main: "#5c6449"
		}
		/* background: {
			default: "#3ec9f3"
		} */
	}
});

export default theme;
